# Triage labels

This repo uses the default canonical triage labels.

- `needs-triage`
- `needs-info`
- `ready-for-agent`
- `ready-for-human`
- `wontfix`

These labels are the default vocabulary expected by the engineering triage skills and can be overridden later if the project adopts a different naming scheme.
